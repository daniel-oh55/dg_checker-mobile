{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "pnpm run types && tsc --noEmit",
    "test": "pnpm run types && vitest run",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "check": "pnpm run typecheck && pnpm run migrate:local && pnpm run test"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
